package top.coolcha.statement.viewholder

import android.view.View
import top.coolcha.statement.databinding.ItemStatementNumberBinding
import top.coolcha.statement.util.BaseStatementViewHolder

class NumberViewHolder(item: View): BaseStatementViewHolder(item) {
    lateinit var binding: ItemStatementNumberBinding
}